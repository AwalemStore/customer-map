name: Data Update
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Update all
        env:
          REWAA_EMAIL: info@paftah.com
          REWAA_PASSWORD: ${{ secrets.REWAA_PASSWORD }}
        run: |
          node scripts/update-report.mjs
          node scripts/fetch-q2-tax.mjs
          node scripts/fetch-q1-returns.mjs
          node scripts/fetch-aging.mjs
          node scripts/customer-analytics.mjs
          node scripts/fetch-dashboard.mjs
          node scripts/fetch-reports.mjs
      - name: Commit
        run: |
          git config user.name "Paftah Bot"
          git config user.email "bot@paftah.com"
          git add paftah-comprehensive-report.html
          git diff --staged --quiet || (git commit -m "auto: $(date -u +'%Y-%m-%d %H:%M')" && git push)
